framework:
    secret: test
    test: true
    router:
        resource: "%kernel.project_dir%/config/routing.yml"

twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
